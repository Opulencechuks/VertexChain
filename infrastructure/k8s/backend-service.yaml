apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: vertexchain
spec:
  selector:
    app: vertexchain-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
