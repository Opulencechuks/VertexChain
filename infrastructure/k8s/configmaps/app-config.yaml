apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: vertexchain-prod
data:
  NODE_ENV: production
  APP_ENV: production
  API_BASE_URL: https://api.vertexchain.app
  LOG_LEVEL: info
  FEATURE_FLAGS: '{"newDashboard":true,"betaReports":false}'
