apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backups-pvc
  namespace: vertexchain-prod
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: vertexchain-ssd
  resources:
    requests:
      storage: 200Gi
