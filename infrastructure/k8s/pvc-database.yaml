apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: database-pvc
  namespace: vertexchain-prod
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: vertexchain-ssd
  resources:
    requests:
      storage: 100Gi
